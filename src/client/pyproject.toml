[build-system]
requires = ["setuptools>=42", "wheel"]
build-backend = "setuptools.build_meta"

[tool.pytest.ini_options]
# Suppress Qt event loop exceptions from napari cleanup
qt_api = "pyqt5"
addopts = "-v"